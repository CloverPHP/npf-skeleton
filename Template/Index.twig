{% extends "Layout/Main.twig" %}
{% block content %}
    <!-- Content Header (Page header) -->
    <div class="content-header">
        <div class="d-flex align-items-center">
            <div class="mr-auto">
                <h3 class="page-title">{{ "Home Page" | i18n }}</h3>
            </div>
        </div>
    </div>

    <section class="content">

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h4>{{ "Welcome" | i18n }} {{ admin.name }}!!!</h4>
                    </div>
                </div>
            </div>
        </div>

    </section>
{% endblock %}